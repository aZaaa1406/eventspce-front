import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['res.cloudinary.com'], // Aquí agregas el dominio de Cloudinary
  },
  /* config options here */
};

export default nextConfig;
